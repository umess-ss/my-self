import { Contact } from "@/components/sections/Contact";

export const metadata = {
  title: "Contact | Umesh Rajbanshi"
};

export default function ContactPage() {
  return (
    <main className="page-shell">
      <Contact />
    </main>
  );
}
